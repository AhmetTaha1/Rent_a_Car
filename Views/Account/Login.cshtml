<div class="container vh-100 d-flex justify-content-center align-items-center">
    <div class="card shadow-sm p-4 log-card" style="max-width: 400px; width: 100%;">
        <h3 class="text-center mb-4">Sign In</h3>
        <form method="post" asp-action="SignIn">
            <input type="hidden" name="returnUrl" value="@ViewBag.ReturnUrl" />
            <div class="mb-3">
                <label for="email" class="form-label fw-semibold text-dark">Email address</label>
                <input type="email" class="form-control border border-secondary text-dark" id="email" name="Email"
                    placeholder="name@example.com" required style="color: black;" />
            </div>
            <div class="mb-3">
                <label for="password" class="form-label fw-semibold text-dark">Password</label>
                <input type="password" class="form-control border border-secondary text-dark" id="password"
                    name="Password" placeholder="Enter your password" required style="color: black;" />
            </div>
            <div class="d-flex justify-content-between align-items-center mb-3">
                <div class="form-check">
                    <input type="checkbox" class="form-check-input" id="rememberMe" name="RememberMe" />
                    <label class="form-check-label text-dark" for="rememberMe">Remember me</label>
                </div>
                <a href="#" class="small text-primary">Forgot Password?</a>
            </div>
            <button type="submit" class="btn btn-primary w-100">Sign In</button>
        </form>
        <div class="text-center mt-3">
            <small class="text-dark">Don't have an account? <a asp-controller="Account" asp-action="Register"
                    asp-route-returnUrl="@ViewBag.ReturnUrl">Sign Up</a></small>
        </div>
    </div>
</div>
